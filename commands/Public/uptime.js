const Discord = require('discord.js');
require('discord-reply');

module.exports = {
  name: 'uptime',
  cooldown: 5,
  aliases: [],
  category: 'Public',
  utilisation: '{prefix}uptime <link>',

  execute(client, message, cmd, args) {
  }
}